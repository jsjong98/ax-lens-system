# =============================================================================
# config.py — API 키 및 모델 설정
# =============================================================================
# OPENAI_API_KEY 를 직접 입력하거나 환경변수로 설정하세요.
# 방법 1: 아래 문자열에 직접 입력
# 방법 2: 터미널에서 export OPENAI_API_KEY="sk-..." 실행 후 사용
# =============================================================================

import os

# ── OpenAI 설정 ──────────────────────────────────────────────────────────────
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "여기에_API_키_입력")

# 사용할 모델 (비용/성능 트레이드오프)
# "gpt-4o"        → 가장 정확, 비용 높음
# "gpt-4o-mini"   → 빠르고 저렴, 충분히 정확 (권장)
# "gpt-3.5-turbo" → 가장 저렴, 정확도 낮을 수 있음
LLM_MODEL = "gpt-4o-mini"

# ── 분류 설정 ─────────────────────────────────────────────────────────────────
# 배치 크기: 한 번에 처리할 Task 수 (API 비용 절감)
BATCH_SIZE = 10

# 동일 Task를 몇 번 호출해서 투표할지 (1=단일 호출, 3=3회 투표 → 안정성↑)
N_VOTES = 1

# API 호출 간 대기 시간 (초) — Rate Limit 방지
SLEEP_BETWEEN_CALLS = 0.5

# ── 출력 설정 ─────────────────────────────────────────────────────────────────
OUTPUT_FILENAME = "분류결과.xlsx"
